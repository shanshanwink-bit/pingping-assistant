# AGENTS.md

## 适用范围与封版原则

- 本文件位于正式项目根目录 `C:\Users\DELL1\Documents\萍萍的小助手`，适用于整个仓库及全部子目录。
- `C:\Users\DELL1\Documents\服装` 是指向正式目录的 Windows Junction，不得删除、重建、重命名或改变目标。
- 项目当前为 1.0.1 封版状态，微信小程序 v1.0.1 已正式上线，用于实习面试展示和上线后维护；默认只处理阻断性 Bug、部署验证、文档和发布事项。
- 历史 v1.0 稳定基线提交 `9237e91` 不得改写。不得以“统一技术栈”或“代码更整洁”为由扩大范围、重构数据库或恢复已废弃功能。

## 产品定位与终端职责

「萍萍小助手」是面向小型零售门店的轻量经营管理系统，以服装经营为主要设计背景，当前同时支持美妆商品。系统由微信原生小程序、Vue 3 Web 管理后台、Node.js 小程序 API、Go 管理后台 API 和 MySQL 组成。

### 微信小程序

- 小程序是高频经营操作端，正式 TabBar 固定为：`首页`、`商品`、`账本`、`我的`。
- 承载今日经营、商品与规格库存、卖货、拿货、记一笔、账本、库存预警、操作记录、AI 商品识别和 AI 拍照入库确认。
- 卖货、拿货和其他收支从首页、商品详情或账本的明确入口进入；已删除的 `quick-action` 页面不得恢复。
- `pages/` 处理页面状态、事件和渲染；共享计算、校验、鉴权与同步放在 `utils/`。同一逻辑第二次复用时提取，不复制函数。

### Web 管理后台

- Web 是低频管理、核对和经营分析端，不作为主要高频录入端。
- 正式导航为：`经营看板`、`商品中心`、`库存中心`、`销售与收支`、`经营分析`、`员工与权限`、`操作日志`。
- `采购与供应商` 和空壳 `基础设置` 不属于正式导航，不得重新加入。
- `admin/src/App.vue` 只负责应用壳和全局装配；页面、布局、公共组件、服务、组合式逻辑和纯工具分别放在现有职责目录。
- HTTP 请求统一放 `admin/src/services/`；组件不得拼接 API 地址、解析数据库字段或实现服务端权限判断。
- 设计令牌位于 `admin/src/styles/tokens.css`；不得新增全局覆盖层或用大量 `!important` 修补页面。

## 商品标识、库存与生命周期

- `itemNumber` / `item_number` 是用户真实货号，来自吊牌、进货单或供应商；可手动填写，也可由 AI 识别辅助，最多 80 字符。
- `code` 是系统内部兼容流水号，只用于历史兼容和内部关联；普通用户界面、搜索结果和普通导出不得把它显示或伪装成真实货号。
- `productId`、`adminProductId`、`specId` 只用于内部关联。
- 新建商品可以按规格填写非负整数初始库存；快速创建未填写库存时为 0。
- 小程序的规格库存保存在 `store_states.products[].specs[]`，Web 的 `admin_products.stock` 是聚合库存。两者粒度不同，必须沿用服务端兼容与事务路径，不在前端自行换算或重构。
- 停用商品不参与普通卖货、拿货、库存调整和普通 AI 匹配；历史记录保留，可由有权限用户重新启用。
- 永久删除仅允许 owner 对零聚合库存、零规格库存、无销售/采购/库存操作/业务审计及处理中 AI 入库引用的误建商品执行。任一历史存在时只能停用；删除不重排、不复用 `code`。

## AI 边界

- AI 商品识别只提取图片中的受控特征并匹配门店真实商品；可以读取图片中明确可见的真实货号。
- AI 拍照入库只生成内存草稿，不自动创建商品或规格，不直接修改库存、价格、成本或账本。
- AI 不生成或决定内部 ID、内部 `code`、真实库存、售价和成本。
- 商品、规格、数量和进价必须由用户核对；只有人工确认后，服务端才通过事务写入库存、采购记录和审计日志。
- 无匹配、重复货号、低置信度、多候选、规格不唯一或停用商品命中时不得自动确认。
- AI 功能受门店 Feature Flag、登录鉴权、图片限制、并发/频率限制和错误脱敏保护；生产密钥只存在服务器环境文件中。

## Demo 面试模式

- Demo 登录由 Node 环境变量显式开启，默认关闭；必须同时固定独立 `DEMO_STORE_ID` 和 `DEMO_USER_ID`。
- Demo 用户必须是 `clerk`，令牌包含 `demo=true`，服务端每次请求重新校验该用户仍属于固定 Demo 店且角色仍为 clerk。
- Demo 只能读取和写入独立 Demo 店，可查看商品、库存和经营数据，也可以卖货、拿货。
- Demo 不得降级到本机其他门店缓存，不得访问真实门店，不得编辑商品资料、永久删除、管理员工、修改基础设置或导出。
- 小程序权限入口可隐藏，但服务端角色与门店校验才是安全边界。Demo 数据必须明确标识为演示数据，不得混入真实经营数据。

## 服务职责与依赖方向

### Node 服务：`server/`

- 负责微信登录、小程序会话、Demo 登录、`store/state`、小程序商品目录、卖货/拿货事务、AI 商品识别和 AI 拍照入库。
- 共享 MySQL 迁移位于 `server/migrations/`。不得删除 Node 的真实业务职责或降级为兼容壳。

### Go 服务：`server-go/`

- 负责 Web 登录与会话、商品管理、聚合库存、经营数据读取、经营分析、员工权限、安全删除和审计日志。
- `main.go` 只负责配置读取、依赖装配和启动；新增可执行程序放 `server-go/cmd/`。
- 依赖方向固定为 `http -> service -> repository`：Handler 不写 SQL，Repository 不处理 HTTP，Service 不依赖路由或响应格式。
- 权限判断在 `server-go/internal/service/` 完成；Vue 隐藏入口不是安全边界。

### MySQL 与经营口径

- Node 与 Go 共享同一 MySQL 和 `storeId`，但使用独立会话。
- `store_states` 与部分 `admin_*` 表是历史形成的双体系；1.0.1 不做 SKU 统一、交易表统一或完整采购系统重构。
- 经营盈利统一为：`可靠成本销售毛利 + 其他收入 - 其他支出`。
- 采购属于现金支出，但不再次从经营盈利扣除；缺少可靠成本的销售计入收入，但不伪造毛利或经营盈利。
- 不得使用占位值、估算值、演示值或前端补值伪造经营数据。

## 鉴权与敏感信息

- 小程序使用微信身份和 Node 签发的会话；Web 使用独立账号和 Go 会话，内部前缀分别为 `/api/v1` 与 `/admin-api/v1`。
- 后台密码仅保存 PBKDF2-SHA256 摘要；服务端会话仅保存随机令牌 SHA-256 摘要。
- 除登录和健康检查外，API 默认要求 Bearer 会话；权限和账号状态在服务端请求时重新校验。
- 店主账号不可被普通管理员降级或停用，也不得停用当前登录账号；权限变化必须写审计。
- 角色模板为 `owner`、`admin`、`finance`、`clerk`。新增权限项须同步迁移默认值、服务端白名单、前端文案和测试。
- 不得提交真实 AppID、AppSecret、OpenID、密码、Token、数据库凭据、DashScope Key、私钥、`.env` 或 `project.private.config.json`。
- 真实 AppID 只保留在本地 `project.config.json`；Git 操作不得覆盖或提交该本地差异，也不得破坏微信开发者工具配置。

## 正式部署状态与路径

- 正式域名、ICP备案、HTTPS 和微信合法域名已完成。
- Web：`https://shanshanwink.online/pingping/`
- Node API：`https://shanshanwink.online/pingping-api/v1`
- Go API：`https://shanshanwink.online/pingping-admin-api/v1`
- Nginx 对外提供 443；Node、Go、MySQL 分别只监听服务器回环地址的 3000、3001、3306。
- Node 目录 `/opt/pingping-assistant-api`，Go 目录 `/opt/pingping-admin-api`，Web 目录 `/var/www/pingping`，生产环境文件 `/etc/pingping-assistant-api.env`。
- 生产数据库已执行到 `009_admin_product_item_number_managed.sql`；`008`、`009` 不得重复执行。迁移文件仍保留用于全新环境按顺序初始化。
- 生产 Demo 面试登录已启用并使用独立 clerk 店铺；变更 Demo 配置前必须重新核对门店和用户，不得复用真实门店。

## 修改原则

1. 封版后只修影响运行、数据正确性、面试体验或发布的明确问题。
2. 修改前阅读现有实现与测试；不大面积重写稳定页面，不扩展规格体系、barcode 或新 AI 功能。
3. 不为了 UI 修改数据库，不新增空壳模块，不恢复废弃入口。
4. 数据库迁移必须先报告意图、范围和回滚方式并取得确认。
5. 线上部署必须先报告本次运行代码差异、部署范围、备份与健康检查；不得把本地完成描述成线上已部署。
6. 不自动写生产测试交易，不修改生产 Nginx、环境文件或数据库，除非任务明确要求并已确认。
7. 不使用 `git reset --hard`、`git clean -fd` 或强制 checkout；不得清除用户本地改动。
8. 页面修改同步检查同目录 `.js`、`.json`、`.wxml`、`.wxss`；API 字段变化同步检查小程序、Vue、Node/Go、迁移和测试。

## 文件规模

- 新增或明显扩展的 `.js`、`.ts`、`.vue`、`.go` 文件接近 300 行时按职责拆分；CSS 接近 400 行时按令牌、布局、组件或页面拆分。
- 单个函数尽量在 50 行以内；取数、业务判断和渲染/响应同时出现时拆分。
- 修改已有大文件只做与当前任务相关的最小拆分，不借机重构无关代码。

## 验证

- Node 全量测试（根目录）：`node --test tests/*.test.js`
- Node API 静态检查（`server/`）：`pnpm run check`
- Vue production build（`admin/`）：`pnpm build`
- Go（`server-go/`）：`gofmt -l .`、`go test ./...`、`go build ./...`
- 小程序 JS：`@('app.js') + (Get-ChildItem custom-tab-bar,pages,utils -Recurse -Filter *.js).FullName | ForEach-Object { node --check $_ }`
- 小程序路由：`node tests/miniprogram-readonly.test.js`
- 页面或配置变化还需在微信开发者工具编译，核对首页、商品、账本、我的、卖货、拿货、记一笔、库存预警、操作记录、Demo 登录和 AI 入库入口；不得写生产测试数据。
- 后台鉴权至少验证未登录 401、登录、`/auth/me`、角色读取、退出后令牌失效；员工权限验证店主保护、停用拒绝登录、无权限 403 和例外权限一致。
- 缺少工具或开发者工具不可用时，必须明确记录未运行项和原因。

## Git 与发布

- 只提交任务范围文件。提交前检查 `git status`、完整 diff、`git diff --check`、暂存区和敏感信息。
- 本地 `project.config.json` AppID 差异必须排除；`.env`、私有配置、构建产物、部署包、截图临时文件和依赖目录不得进入提交。
- 提交和 push 是两项独立动作；只有用户明确授权时才能提交或 push，禁止 force push。
- 发布后确认本地 `main` 与 `origin/main` 一致、working tree 除本地 AppID 差异外无未提交内容，并记录生产是否同步。
